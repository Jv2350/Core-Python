# Python program to sum all the items in a list:

input_list = list(map(int, input("Enter numbers separated by spaces: ").split()))

print(f"Sum of all items: {sum(input_list)}")  # sum funciton to calculate the total
